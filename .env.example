# Neon Postgres (copy from your Neon dashboard)
DATABASE_URL=postgresql://user:password@host/catechumen?sslmode=require

# JWT secret — generate with: openssl rand -hex 32
JWT_SECRET=your-secret-here

# Optional: bcrypt work factor (default 12)
BCRYPT_ROUNDS=12
